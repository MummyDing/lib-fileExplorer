apply plugin: 'com.android.library'

android {
    compileSdkVersion 21
    buildToolsVersion "21.0.2"
}


sourceSets {
    test {
        java.srcDirs = ['tests/unitTest']
    }
    androidTest {
        java.srcDirs = ['tests/androidTest']
    }

}

dependencies {
    compile project(path: ':YMBase')
}
